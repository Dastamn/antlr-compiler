import Small_Java.lang;
import Small_Java.io;

class_SJ Test {
	int_SJ  i, j, k;
    main_SJ {
        i := 7 - 4;
        j := 5 + 2 / 7;
        k := 5;
        Si (!(i < j) & !(k != j)) Alors {
            i := 0 + 1;
        } Sinon {
            j := 2 + 1;
        }
        k:= 2;
    }
}