import Small_Java.lang;
import Small_Java.io;

class_SJ Test {
	int_SJ  i, j;

    main_SJ {

        i := 1;
        j := 5 + (2 + 6) / 5;

        Si (!(1 < 2) & !(7 != 8)) Alors {
            i := 0 + 1;
        } Sinon {
            j := 2 + 1;
        }
    }

}