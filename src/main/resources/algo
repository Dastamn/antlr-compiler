import Small_Java.lang;
import Small_Java.io;

class_SJ Test {
	int_SJ  a, b, c, d, x;
    main_SJ {

        a := 1;
        b := 2;
        c := 3;

        x := a * ((a * b + c) - (c * -b));

    	Si (!(5 + 1 > -3 & 5 < 2 | 10 = 7)) Alors {
            x := 5;
    	} Sinon {
    	    x := 5 + 9;
    	}
    }
}